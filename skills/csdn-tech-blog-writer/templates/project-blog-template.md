# 【{{系列名}}|第{{篇号}}篇】{{模块名称}}实现

## 前言

用 2 到 5 段说明：

- 前面已经完成了什么模块，本篇继续实现什么模块。
- 本篇模块为什么值得单独写，例如比基础 CRUD 多了分页、条件查询、关联查询、业务校验、统计接口等。
- 本篇会按照“小功能需求 -> 三层代码 -> 文字说明 -> 知识点”的方式展开。

前言示例语气：

> 今天我们来学习 {{模块名称}} 模块的实现。
>
> 相比前面已经完成的 {{前置模块}}，{{模块名称}} 这一部分会更贴近真实业务一些。因为它不仅要实现基础的增删改查，还要完成 {{核心扩展点1}}、{{核心扩展点2}}、{{核心扩展点3}}。

## 一、{{模块名称}}模块实现

如果本模块依赖实体类、参数类、分页结果类或统一返回类，先写准备工作。

### 0. 准备工作 / pojo 层

```java {{实体或参数类路径}}
{{核心字段代码}}
```

### 1. {{小功能名称}}

#### 1.1 功能需求

说明前端页面需要什么、后端接口要完成什么，例如：

- 按名称模糊查询
- 按状态筛选
- 分页展示结果
- 关联展示其他表字段

#### 1.2 Controller 层实现

```java {{Controller 文件路径}}
{{Controller 核心方法}}
```

#### 1.3 Service 层实现

```java {{Service 文件路径}}
{{Service 核心方法}}
```

#### 1.4 Mapper 层实现

如果是注解 SQL，使用 Java 代码块；如果是 XML SQL，使用 XML 代码块。

```xml {{Mapper XML 文件路径}}
{{Mapper 或 Mapper XML 核心代码}}
```

#### 1.5 文字说明

紧扣三层代码解释：

- Controller 层接收了什么参数，返回了什么结果。
- Service 层处理了哪些业务逻辑，例如补时间、默认值、校验、分页、事务。
- Mapper 层执行了什么 SQL，是否涉及动态 SQL、关联查询、分组统计。
- 这个功能在前端页面中通常对应什么场景。

#### 1.6 涉及知识点

只讲当前功能真实涉及的技术点，例如：

1. `PageHelper` 分页
2. MyBatis 动态 SQL：`<where>`、`<if>`、`<set>`、`<foreach>`
3. `LEFT JOIN` 关联查询
4. `CASE WHEN` 编码转换或状态计算
5. `@RequestBody` / `@PathVariable` / `@RequestParam`
6. 业务校验、全局异常处理或事务

### 2. {{第二个小功能名称}}

按照同样结构继续：

- 功能需求
- Controller 层实现
- Service 层实现
- Mapper 层实现
- 文字说明
- 涉及知识点

## 二、{{可选：统计功能 / 异常处理 / 事务处理}}

当模块中包含统计、异常处理、事务、一对多映射等内容时，单独成章。

### 1. {{功能名称}}

#### Controller 层

```java
{{Controller 代码}}
```

#### Service 层

```java
{{Service 代码}}
```

#### Mapper SQL

```xml
{{SQL 代码}}
```

#### 知识点解析

解释当前功能和普通 CRUD 的区别，例如统计接口更关注返回结构是否适合前端图表展示。

## 三、{{模块名称}}核心知识点总结

围绕本篇实际出现的知识点写，不要泛泛而谈。

### 1. 三层架构分工

`Controller`：接收请求、返回结果。

`Service`：处理业务逻辑、默认值、校验、事务、状态变更。

`Mapper`：执行 SQL，完成动态查询、关联查询、统计查询。

### 2. {{核心知识点}}

结合本篇代码解释。

### 3. {{核心知识点}}

结合本篇代码解释。

## 总结

总结要换一种自然收尾方式，不要只罗列功能清单。

推荐写法：

- 先说明“到这里，{{模块名称}} 的核心功能已经梳理完成”。
- 再说明它相比基础 CRUD 多了哪些真实业务点。
- 最后说明通过这一篇能更清楚理解三层架构如何落到业务开发中。

推荐标签：

- Spring Boot
- MyBatis
- JavaWeb
- Tlias
- 后端
